---
scope: 2026 Agent Workflow initiative
purpose: build-substrate
altitude: initiative
updated: 2026-08-01T03:47
---

- (direction) Interactive Coaching path; spine-only build substrate; no additional audience artifact.
- (constraint) SPEC-2026-agent-workflow and its companions are the canonical capability contract; architecture must not silently weaken or pre-decide its explore set.
- (decision) [ADOPTED] Python monolith with DDD; one backend owns agent flow, REST, and data import. Binds all capabilities; prevents TypeScript/Python service splits and duplicated domain ownership.
- (constraint) Security hardening and threat modeling are outside this initiative; do not let that omission erase basic safe boundaries.
- (decision) [ADOPTED] All ingest modes enter one staged discover/create, assign, extract pipeline with separate prompts and independently scored stage outcomes. Binds CAP-1/CAP-2; prevents mode-specific paths and same-call create+assign.
- (decision) [ADOPTED] The taxonomy is a tree of substitutability categories: exactly one leaf per product, facets within leaves, parents widen comparison. Binds CAP-3; prevents graph semantics and retailer taxonomy becoming the comparison primitive.
- (decision) [ADOPTED] Non-null extracted traits and inferred quantities require verbatim evidence_span; deterministic validators reject unsupported values and unknown/defer becomes a durable review item. Binds CAP-4/CAP-5; prevents fabricated structured data.
- (decision) [ADOPTED] Category context is obtained through search over a single command layer; MCP is the first thin adapter, with CLI/Code Mode adapters deferred until measured limits. Binds CAP-6 and capability surface; prevents full taxonomy dumps and domain logic in protocol adapters.
- (decision) [ADOPTED] Product writes merge deterministic and validated LLM-owned fields and never erase prior LLM values on re-import. Binds CAP-7; prevents destructive whole-record replacement.
- (decision) [ADOPTED] Taxonomy changes prefer existing categories, allow real eager creation, and are corrected by periodic and post-bulk hygiene; no draft category lifecycle. Binds CAP-8; prevents provisional state and taxonomy fragmentation without reconciliation.
- (decision) [ADOPTED] HITL is non-blocking and domain-owned: first-party review UI over Postgres deferred_items with re-drive commands; Phoenix annotations remain trace labels. Binds CAP-9; prevents pipeline waits and vendor queues owning domain actions.
- (decision) [ADOPTED] Arize Phoenix self-hosted is the single trace, dataset, experiment, and run UI platform, using a dedicated database in existing Postgres 18; domain depends only on a thin OpenTelemetry API port. Binds CAP-10 and observability; prevents vendor SDK coupling in domain and duplicate trace stores.
- (constraint) Control-flow ownership O4/O5 and product supply P2/P3/P4/P9/P10 are bakeoff-gated; the architecture must provide neutral interchangeable seams and equal resumability without selecting a winner.
- (question) Open from SPEC: category coherence timing is unresolved between per-assign checks and batched reconciliation.
- (question) Open from SPEC: reconciliation dirty-set trigger is unresolved among timestamps, flags, queue/event records, or audit-derived worksets.
- (constraint) Brownfield reality: current system is separate NestJS backend and TypeScript importer sharing Postgres; combined discover/assign, full taxonomy prompts, flat categories, scalar normalized units, and importer replacement writes are rebuild targets, not conventions to preserve.
- (constraint) Durable existing seams to preserve conceptually: Postgres 18, product identity by name+source+URL, source/import category distinct from substitutability category, deterministic unit extraction, CSV batch streaming, and the existing frontend's OpenAPI consumption.
- (question) Brownfield cutover and data posture remain open: greenfield schema vs migration, TypeScript replacement strategy, existing categorized data fate, and frontend API compatibility.
- (question) Operational envelope remains open: migration ownership, API versus worker process for bulk jobs, devcontainer/compose topology, and production deployment boundary.
- (decision) [ADOPTED] Use a hexagonal modular monolith organized into vertical domain-capability packages. Pure domain code imports no FastAPI, Pydantic, SQLAlchemy, agent/orchestration framework, Phoenix, or OpenTelemetry; application commands coordinate ports; all external technologies are adapters. Binds all capabilities and bakeoff seams; prevents framework-owned domain rules and incompatible adapter-specific use cases.
- (decision) [ADOPTED] Domain operations receive facts and return new state, rejection, and/or un-published domain events; repositories, transactions, clocks, IDs, telemetry, and event publication remain application or adapter responsibilities. Binds deterministic kernel; prevents hidden side effects in domain decisions.
- (decision) [ADOPTED] Domain/application own a telemetry port; OpenTelemetry API and SDK imports live in the infrastructure adapter. Binds observability; prevents the source SPEC's OTel wording from becoming a framework dependency in the pure domain.
- (decision) [ADOPTED] Split product-related state by durable owner: catalog owns source identity/raw/imported facts and shelf price; taxonomy owns category tree and product-to-leaf membership; enrichment owns cited traits and quantities; review owns deferred items and re-drive lifecycle; pipeline coordinates but owns no business records; comparison-ready reads compose these stores. Binds CAP-1/CAP-3/CAP-4/CAP-5/CAP-7/CAP-9; prevents re-import from structurally reaching or wiping agent-owned state and prevents multiple modules owning one mutable row.
- (decision) [ADOPTED from command-layer constraint] All state mutation enters through application command handlers that invoke domain validation and owner-specific repositories in a transaction. Agents, REST/MCP/CLI adapters, workflow drivers, and orchestration bakeoff arms never access repositories or SQL directly. D1 and D2 remain valid experiments by varying who invokes the same command and when, not by creating alternate write paths. Binds CAP-2/CAP-7/CAP-8/CAP-9/CAP-10; prevents persistence semantics from varying by control-flow arm.
- (decision) [ADOPTED] Replace the legacy TypeScript data model side-by-side: the Python monolith owns a clean Alembic-managed application schema, replays source catalogs rather than migrating legacy AI-derived categories/attributes, keeps the old path read-only for comparison until capability/eval gates pass, then switches consumers and retires it. Binds data cutover and CAP-1/CAP-7/CAP-10; prevents semantic legacy assumptions from contaminating the new taxonomy while preserving rollback.
- (decision) [ADOPTED] One Python package/container image runs as separate API and worker process roles. API/REST/MCP and CLI submit application commands or durable jobs; workers execute bulk, filtered, single-item, hygiene, and re-drive work. Postgres stores queue/run/stage checkpoints with idempotent claiming and recovery; no Redis or external workflow runtime is required. Binds CAP-1/CAP-2/CAP-8/CAP-9/CAP-10 and all O4/O5 arms; prevents long jobs from coupling API availability to pipeline execution and gives every bakeoff arm equal resumability.
- (decision) [ADOPTED] Taxonomy coherence is asynchronous and category-scoped: assignment performs structural and per-product semantic checks, then transactionally upserts one coalesced taxonomy review work item for each affected category. The queue records reason codes, first/latest trigger time, change count/generation, run/trace references, status, lease, and attempts; a generation watermark prevents changes arriving during review from being lost. Binds CAP-8 and SPEC coherence/dirty-set questions; prevents full group-level LLM checks on every assignment and duplicate review jobs during bulk work.
- (decision) [ADOPTED] No domain-event outbox/journal for taxonomy review. Current application tables are authoritative, the durable work queue carries concise trigger facts, and Phoenix traces provide detailed assignment history through stable run/trace references. Binds hygiene context and observability; prevents duplicated trace payloads and generic event-consumer machinery while retaining reliable review triggering.
- (decision) [ADOPTED] Select one MVP orchestration framework now on maturity, workflow expressiveness, durability, MCP/tool support, Phoenix-compatible OTel, and Python fit; production orchestration may couple directly to it and no generic swappable-driver abstraction is required. Binds MVP stack and pipeline; prevents premature framework-independence abstractions.
- (decision) [ADOPTED] CAP-10 compares workflow designs and product-supply strategies implemented within the selected framework, measuring quality, efficiency/cost, and speed on the same datasets and controls; O4/O5 are workflow shapes, not competing framework vendors. Shared domain commands, current-state persistence, Phoenix instrumentation, and eval definitions remain constant across experiments. Binds CAP-10; prevents framework differences from confounding workflow experiments.
- (direction) Source SPEC wording should be clarified during reconciliation: control-flow bakeoff does not prohibit selecting the orchestration framework before workflow experiments.
- (decision) [ADOPTED] LangGraph 1.2.10 owns MVP workflow orchestration and may be used directly without a framework-neutral driver abstraction. Workflow variants and product-supply strategies are separate LangGraph graphs/configurations evaluated under shared controls. LangGraph state is orchestration-only; business state remains in owner tables and all mutations call application commands. Binds CAP-1/CAP-2/CAP-10; prevents graph checkpoints from becoming a second domain database or workflow experiments duplicating business behavior.
- (version) LangGraph 1.2.10 verified on PyPI 2026-07-31; official docs verify deterministic+agentic workflows and production Postgres checkpointers; Phoenix official docs verify first-class tracing via openinference-instrumentation-langchain.
- (decision) [ADOPTED] Durability is split by responsibility: application-owned Postgres run/job/queue records schedule, debounce, throttle, lease, retry, and report overall work; LangGraph AsyncPostgresSaver stores only internal graph checkpoints and resumes a claimed workflow, keyed by the application run ID. Domain tables remain authoritative and stage payloads are not duplicated in a custom checkpoint store. Binds worker runtime and CAP-1/CAP-8/CAP-9; prevents treating checkpoints as a scheduler or maintaining two copies of graph state.
- (constraint) Do not adopt LangSmith Agent Server for MVP: standalone OSS LangGraph checkpointers do not provide queue scheduling, while self-hosted Agent Server requires an Enterprise license and Redis/Valkey. Phoenix remains observability/eval; application workers remain first-party.
- (decision) [ADOPTED] PgQueuer 1.3.2 owns generic Postgres-backed task dispatch for pipeline runs, re-drive, scheduled maintenance, and cleanup launch. Application pipeline_runs/deferred_items/taxonomy_review_requests remain authoritative; PgQueuer handlers invoke LangGraph and application commands, never domain persistence directly. Jobs enqueue in the same transaction as triggering business state. Binds API/worker runtime; prevents custom lease/retry infrastructure and dual-write job loss without making the queue a second workflow engine.
- (constraint) PgQueuer delivery is at-least-once under crash recovery despite exclusive claims; every task carries an application run/idempotency key and handlers must tolerate replay. Taxonomy trailing-edge debounce remains generation-based application state, and LLM provider rate limiting remains an adapter concern rather than a queue guarantee.
- (version) PgQueuer 1.3.2 verified on PyPI 2026-07-31; MIT, Python >=3.10, psycopg/asyncpg drivers, transactional enqueue, SKIP LOCKED claims with LISTEN/NOTIFY plus polling fallback, scheduling, completion tracking, Prometheus/OTel, and dashboard.
- (question) Implementation spike must verify PgQueuer atomic enqueue on the chosen application transaction/driver, multi-worker global concurrency, SIGKILL recovery with LangGraph resumption, cancellation, trace propagation, and queue retention/autovacuum behavior before relying on it for completion guarantees.
- (decision) [ADOPTED] Use SQLAlchemy 2 async repositories and an application Unit of Work over Psycopg 3, with Alembic owning all application-schema migrations. SQLAlchemy persistence models remain adapter-local and map explicitly to pure domain objects; Pydantic DTOs are not ORM/domain entities. PgQueuer enqueue joins the same underlying transaction as triggering state. Binds all owner modules and job dispatch; prevents implicit cross-module writes, dual commits, and framework-coupled domain models.
- (version) Persistence versions verified on PyPI 2026-07-31: SQLAlchemy 2.0.51, Alembic 1.18.5, Psycopg 3.3.4.
- (decision) [ADOPTED] One API process hosts FastAPI REST and a mounted FastMCP Streamable-HTTP ASGI app at /mcp. Both are curated thin adapters over the same application commands/queries, transaction boundary, and telemetry; MCP tools are designed explicitly for agents and are not auto-generated from the OpenAPI surface. Binds CAP-1/CAP-6/CAP-9 and O1; prevents REST-shaped tool bloat, duplicated domain behavior, and a second service boundary.
- (version) Inbound versions verified on PyPI 2026-07-31: FastAPI 0.141.1 and FastMCP 3.4.5; FastMCP official docs verify ASGI mounting and recommend curated MCP tools over mirroring OpenAPI for agent performance.
- (decision) [ADOPTED] The application LLM port is implemented by LangChain provider chat-model adapters for OpenAI, Anthropic, and Google; workflows depend only on the application port/configuration and use no LangChain agents, memory, persistence, or domain types. Model, prompt, workflow, and supply-strategy IDs are explicit run inputs and Phoenix attributes. Binds CAP-2/CAP-6/CAP-10; prevents provider SDK differences and hidden model defaults from confounding workflow experiments.
- (version) LangChain provider versions verified on PyPI 2026-07-31: langchain-core 1.5.3, langchain-openai 1.4.1, langchain-anthropic 1.5.3, langchain-google-genai 4.3.2; Pydantic 2.13.4 is their common structured-data boundary.
- (decision) [ADOPTED] Every application-owned entity and run uses an application-generated UUIDv4 primary/public identity. Do not use sequential IDs or time-ordered UUIDv7 because identifier order/timestamps can leak account age and activity/volume signals; product source keys remain separate uniqueness constraints. PgQueuer/LangGraph/Phoenix internal identifiers are opaque adapter data and never cross as domain/API IDs. Binds all modules and formats; prevents mixed domain ID types and business-intelligence leakage through identifiers.
- (decision) [ADOPTED] The Python monolith targets Python 3.14.6 and uses uv 0.12.0 with committed pyproject.toml and uv.lock for dependencies, tools, scripts, and Python installation. Yarn remains only for frontend and read-only legacy TypeScript components; the workspace Yarn-only rule must be narrowed accordingly. Binds all Python build/test/runtime environments; prevents dual Python package managers and Yarn wrappers around Python tooling.
- (version) Python 3.14.6 and uv 0.12.0 verified current stable releases on python.org/PyPI 2026-07-31.
- (decision) [ADOPTED] Relocate the current TypeScript backend and data-importer under legacy/ and establish /backend as the permanent Python monolith root at rebuild start; frontend remains at /frontend. Binds repository structure and cutover; prevents a temporary backend-v2 rename or permanent technology-suffixed component path.
- (decision) Operational seed: one backend image runs api, worker, and migrate commands; devcontainer/Compose runs API, worker, Postgres 18, and Phoenix. The new application and Phoenix use separate databases in the same Postgres instance; legacy DB stays read-only during cutover. Production provider and autoscaling target are deferred until deployment work, but must preserve the same image/process-role contract.
- (decision) Consistency conventions: UUIDv4 application IDs; UTC timestamptz timestamps; Decimal/NUMERIC for money and quantities, never binary floats; lowercase snake_case persisted/API enum values; every external/job command carries an idempotency key; current-state owner tables are authoritative and framework/vendor tables are adapter-private.
- (decision) Verification seed: pure-domain unit tests require no framework/database; repository/UoW/queue/checkpoint adapters run against disposable Postgres 18 via Testcontainers; REST and MCP contract tests prove equivalent command behavior; stage quality/cost/speed are Phoenix dataset experiments, not ordinary unit-test assertions.
- (constraint) API/MCP remain internal or local until a later security/auth initiative; security hardening is out of scope but no unauthenticated public MCP mutation surface may be deployed by default.
- (question) Deferred operational choices: production provider, autoscaling quantities, connection pooler, queue retention/autovacuum thresholds, and final deployment IaC; revisit before non-local deployment.
- (version) Phoenix 19.11.1 verified current on PyPI 2026-07-31; Elastic-2.0, Python >=3.10,<3.15. The 15.10.0 gen_ai-to-OpenInference conversion remains the compatibility floor.
- (decision) [ADOPTED] Durable catalog identity is (source_namespace, source_product_id, optional source_variant_id). Name, raw/canonical URL, and retailer presentation are mutable imported facts, not identity. Each source adapter owns extraction/normalization of stable IDs; missing or colliding stable IDs fail/defer under a versioned adapter policy rather than silently falling back to mutable presentation. Binds catalog and all downstream references; prevents duplicate products after rename/URL changes and accidental variant merges.
- (decision) [ADOPTED] Normalized comparison price is authoritatively derived from exact shelf-price and selected quantity revisions. Efficient reads use a rebuildable indexed comparison projection containing source revision IDs, comparable unit, formula/version, and normalized price; no naked cache lives on quantity rows. Binds CAP-5 and comparison reads; prevents stale price/quantity combinations while allowing indexed sort/filter.
- (version) Reviewer-gate verification 2026-07-31: uv 0.12.1, PostgreSQL 18.4, langgraph-checkpoint-postgres 3.1.1, psycopg-pool 3.3.1, OpenTelemetry SDK 1.44.0, openinference-instrumentation-langchain 0.1.68, and Uvicorn 0.52.0 are current; full application set resolves on CPython 3.14.6.
- (event) Reviewer-gate reconciliation tightened canonical contracts, catalog/evidence revisions, comparison projection, idempotency/retry, telemetry, FastMCP lifecycle, vendor migration order, cutover evidence, and operational deferrals.
- (decision) [ADOPTED] CAP-10 bakeoffs isolate mutable state by cloning/restoring one immutable Postgres baseline into a disposable database per candidate. The baseline contains frozen catalog observations, versioned embeddings, taxonomy/config versions, and checksum identity. Candidates never share a live mutable owner DB; embeddings are generated once into the baseline and treated read-only during experiments, so API cost is paid once while temporary storage is duplicated. Cleanup deletes candidate DBs. Binds CAP-10; prevents later candidates observing earlier candidate taxonomy/enrichment writes.
- (decision) [ADOPTED] A retrieval package owns revisioned product embeddings in pgvector inside the application database. Embedding identity is (observation revision, model/version, preprocessing version); distance metric and index type are versioned with the embedding config. CAP-1 filters and P-strategy candidates consume the same versioned embedding snapshot. A separate live shared retrieval DB is deferred until measured storage/clone cost justifies it. Binds CAP-1 and P bakeoffs; prevents incompatible embedding stores/models across units.
- (version) pgvector extension 0.8.6 and pgvector-python 0.5.0 verified current 2026-08-01; Postgres 18 compatible.
- (event) Applied reviewer-gate closure: bakeoff DB-per-candidate isolation, pgvector retrieval AD-35, enrichment freshness, fail-closed projection, stage_execution_id, durable stage outcomes, OTLP exporter pin, and operational bootstrap/privilege deferrals.
- (version) opentelemetry-exporter-otlp-proto-http 1.44.0 verified on PyPI 2026-08-01.
- (event) Produced interactive HTML architecture walkthrough at architecture-walkthrough.html (15 slides) for team discussion/onboarding.
- (event) Spine finalized: lint pass (0 findings), reviewer-gate closure applied (AD-20 bakeoff isolation, AD-35 pgvector retrieval, AD-15 observation window), status set to final.
