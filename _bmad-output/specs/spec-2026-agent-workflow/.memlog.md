---
topic: 2026 agent workflow — comparison-ready product DB pipeline
goal: Structured substitutability categories + traits + comparable prices via staged agent ingest
updated: 2026-07-31T22:55
---

- (note) Sources: docs/specs/2026-scope.md + brainstorm-tasks.md; absorb morphological-matrix.md as adopted companion for morph IDs
- (direction) Express distill from rich pre-sorted scope + brainstorm handoff; no guided elicitation
- (decision) Slug=2026-agent-workflow from brainstorm-2026-agent-workflow-2026-07-30; specs live under _bmad-output/specs/
- (decision) Near-term product: comparison-ready row = substitutability category + traits + shelf price + normalized comparable price; not shopper-facing yet
- (decision) Stack: Python monolith + DDD; one backend for agent flow, REST, and data import; Devcontainers
- (constraint) Security explicitly out of scope for this initiative
- (constraint) Design for generic catalogs beyond Swiss grocery CSVs with minimal adaptation
- (direction) Automation maximized; humans only where judgment is needed; non-blocking HITL only
- (capability) CAP-1 ingest: bulk, single upsert, filtered subset share one pipeline (U1–U4)
- (capability) CAP-2 staged pipeline: separate discover/create, assign, extract prompts and eval slices (I9)
- (capability) CAP-3 taxonomy: many-to-one product↔leaf; tree (not graph); consumer-fine leaves + facets + parents for widen (G2+G5+G6)
- (capability) CAP-4 traits: mandatory evidence_span on non-null; unknown/defer→DLQ (I3, H2); runtime faithfulness beyond span left to eval
- (capability) CAP-5 units/prices: preferred_comparable_unit on compare leaves + quantities[]; no silent fake normalize (T4+T5); T1 deterministic-first extract
- (capability) CAP-6 category context via agent search (C2); no full taxonomy dump; mature lean C4
- (capability) CAP-7 persist: merge upsert never wipes LLM attributes on re-import (D6 hard requirement)
- (capability) CAP-8 create/hygiene: prefer match-existing (I7) then eager create (N1); no draft categories (N2); M2+M5 hygiene ops incl. coherence
- (capability) CAP-9 non-blocking HITL: async low-confidence dashboard (H1) + DLQ (H2)
- (capability) CAP-10 eval/bakeoff: E1 golden assign, E2 per-stage, E3 cost/item, E5 model/context; explore O1–O5 and P2/P3/P4/P9/P10 under same harness — do not pre-pick
- (decision) Substitutability = prose rubric + few-shot pairs (S1+S2); comparison primitive = consumer substitutability not retailer taxonomy
- (decision) Import/source category = filter only; substitutability category = comparison primitive
- (decision) T3 (force all into category unit) killed; T2 LLM-only not MVP default for extract
- (question) Category coherence timing: per-assign full check vs batched reconciliation (lean: reconciliation required; per-assign optional)
- (question) Reconciliation dirty set: how to trigger hygiene without full DB scan (timestamps, dirty flags, event queue, audit-derived)
- (question) Inspectability at MVP: custom agent-run UI (E4) vs structured logs+eval only; prefer vendor free inspectability first
- (assumption) Assumed formal specs path is _bmad-output/specs/ (this run); .kiro no longer canonical for new work
- (assumption) Assumed morph matrix remains adopted companion so bakeoff design keeps ID vocabulary without duplicating full matrix into kernel
- (note) Companions: glossary.md, architecture-diagrams.md, explore-bakeoffs.md, kill-pile.md; adopted morphological-matrix.md
- (decision) Secondary normalized prices not stored separately — use other quantity rows or derive on read
- (note) Wrapper-only drops: current-workflow step list; current-issues laundry (pain absorbed in Why); long-term fine-tuning later; external blog URLs (kept in source/brainstorm only); brainstorm checklist ceremony
- (event) Self-validate pass 1 (coherence): Spec Law 1–6,8 OK — 10 caps with intent+success; constraints bend design; non-goals+kill-pile explicit; success signal demonstrable; CAP IDs CAP-1..10 unique
- (event) Self-validate pass 2 (preservation): load-bearing commitments, explore set, kill pile, taxonomy/units/HITL/D6, open Qs landed in SPEC or companions; morph matrix adopted; verdict=pass
- (event) Spec create complete; folder _bmad-output/specs/spec-2026-agent-workflow
- (decision) Architecture sync: LangGraph is selected as the MVP framework; CAP-10 compares O4/O5 workflow shapes and P strategies inside that framework, not orchestration vendors.
- (decision) Architecture sync: domain/application own a telemetry port and import no OpenTelemetry package; OTel API/SDK/exporter and auto-instrumentation live in infrastructure adapters.
- (decision) Architecture sync: category-wide coherence is asynchronous; affected categories are transactionally coalesced into taxonomy_review_requests with generation watermarks and Phoenix trace references, without a domain-event journal.
- (event) Canonical SPEC and architecture-diagrams synchronized from architecture coaching decisions on 2026-07-31.
- (decision) Architecture sync: durable source identity is source namespace plus stable source product/variant ID; mutable name and URLs are observations, and missing stable IDs explicitly defer/fail.
- (decision) Architecture sync: normalized price is derived from exact shelf-price and quantity revisions; a rebuildable revision-linked indexed comparison projection supports efficient sort/filter, with no quantity-row cache.
